import 'package:flutter/material.dart';

class VirementOrange extends StatelessWidget {
  const VirementOrange({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}